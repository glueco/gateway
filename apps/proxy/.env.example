# Environment Variables for Glueco Gateway Proxy
# Copy this file to .env and fill in the values

# ===========================================
# REQUIRED: Database (Prisma)
# ===========================================
DATABASE_URL="postgresql://user:password@localhost:5432/gateway?schema=public"

# ===========================================
# REQUIRED: Redis (Upstash)
# Used for rate limiting and nonce cache
# ====================================A self-hostable gateway for safely sharing access to private resources with cryptographic proof-of-possession identity.

=======
UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ===========================================
# REQUIRED: Encryption
# Master key for secrets encryption (32+ bytes, base64 encoded)
# Generate with: openssl rand -base64 32
# ===========================================
MASTER_KEY="generate-a-secure-random-key-here"

# ===========================================
# REQUIRED: Gateway URL
# Base URL for this gateway (used in pairing strings)
# ===========================================
GATEWAY_URL="http://localhost:3000"

# ===========================================
# OPTIONAL: Admin Authentication
# If set, required for admin API endpoints
# If not set, admin endpoints are open in development mode only
# ===========================================
# ADMIN_SECRET="your-admin-secret"
